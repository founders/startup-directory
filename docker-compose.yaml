version: '3.7'

services:
  frontend:
    restart: always
    build: ./app
    container_name: startup-dir
    command: yarn dev
    volumes:
      - ./app:/app/startup-dir
    ports:
      - 3000:3000
  db:
    image: mongo
    container_name: db
    ports:
      - '27017:27017'

volumes:
  node_modules:
